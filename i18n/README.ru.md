[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# Голосовой чат-бот с использованием Whisper, LLaMA и Groq API

![Python](https://img.shields.io/badge/Python-3.7%2B-3776AB?logo=python&logoColor=white)
![Whisper](https://img.shields.io/badge/STT-OpenAI%20Whisper-1f6feb)
![Groq](https://img.shields.io/badge/LLM-Groq%20LLaMA%203--8B-FF6B35)
![gTTS](https://img.shields.io/badge/TTS-Google%20gTTS-34A853)
![UI](https://img.shields.io/badge/UI-Gradio-F97316)
![Status](https://img.shields.io/badge/Project-Active%20Prototype-2ea44f)

Этот проект представляет собой голосовой чат-бот в реальном времени, который использует Whisper от OpenAI для преобразования речи в текст, LLaMA 8B через Groq API для генерации ответов и Google Text-to-Speech (gTTS) для обратного преобразования текста в речь. Интерфейс чат-бота построен на Gradio, что позволяет пользователям взаимодействовать с ботом голосом или загружая аудиофайлы.

## Обзор

Приложение реализует полный цикл аудио-диалога в одном Python-скрипте:

1. Принимает пользовательское аудио с микрофона или из загруженного файла.
2. Преобразует речь в текст с помощью Whisper (модель `base`).
3. Генерирует ответ через Groq (`llama3-8b-8192`).
4. Преобразует текст ответа обратно в MP3-аудио с помощью gTTS.
5. Возвращает и текст ответа, и воспроизводимое аудио в веб-интерфейсе Gradio.

### Конвейер диалога

| Этап | Компонент | Результат |
|---|---|---|
| 🎙️ Ввод | Gradio Audio (mic/file) | Путь к аудио |
| 📝 Транскрибация | Whisper `base` | Текст пользователя |
| 🧠 Обработка | Groq + `llama3-8b-8192` | Текст ассистента |
| 🔊 Синтез | gTTS | MP3-ответ |
| 🖥️ Выдача | Gradio UI | Текст + воспроизводимое аудио |

## Возможности

- **Речь в текст (Speech-to-Text)**: Преобразование устной речи в текст с помощью модели Whisper от OpenAI.
- **Ответы, сгенерированные ИИ**: Использование LLaMA 8B через Groq API для генерации осмысленных ответов на основе распознанного текста.
- **Текст в речь (Text-to-Speech)**: Преобразование сгенерированных текстовых ответов обратно в речь с помощью Google Text-to-Speech (gTTS).
- **Взаимодействие в реальном времени**: Чат-бот работает в реальном времени, позволяя пользователям взаимодействовать через микрофон или загружая аудиофайлы через веб-интерфейс.
- **Простой запуск из одного файла**: Весь конвейер чат-бота реализован в `voice_to_voice_chatbot.py`, что удобно для экспериментов.

## Структура проекта

```text
Voice-to-text-and-voice-chatbot/
├── voice_to_voice_chatbot.py               # Main script to run the chatbot
├── requirements.txt                        # List of Python dependencies
├── i18n/                                   # Localized READMEs (planned/generated in pipeline)
└── .auto-readme-work/20260228_230442/      # README automation context/artifacts
```

Справка по устаревшей структуре из канонического README:

```text
voice-to-voice-chatbot/
├── voice_to_voice_chatbot.py  # Main script to run the chatbot
├── requirements.txt           # List of Python dependencies
├── README.md                  # Project documentation
└── .gitignore                 # Git ignore file
```

## Предварительные требования

Перед началом убедитесь, что выполнены следующие требования:

- Установлен Python 3.7 или выше на локальной машине или в Google Colab.
- Есть ключ API Groq. Получить ключ можно [здесь](https://groq.com/).
- [Google Colab](https://colab.research.google.com/) или локальная среда Python с установленными необходимыми библиотеками.
- Доступ в интернет для:
  - Первичной загрузки модели Whisper.
  - Вызовов Groq API.
  - Генерации аудио через gTTS.

### Требования вкратце

| Требование | Зачем это нужно |
|---|---|
| Python `3.7+` | Среда выполнения для скрипта чат-бота и зависимостей |
| Groq API Key | Аутентифицированный доступ к инференсу модели LLaMA |
| Colab or Local Env | Среда выполнения для Gradio + ML-библиотек |
| Internet Access | Загрузка модели Whisper, запросы Groq, синтез gTTS |

## Установка

Выполните следующие шаги для настройки проекта:

1. **Клонируйте репозиторий:**

```bash
git clone https://github.com/aquibali01/voice-to-voice-chatbot.git
cd voice-to-voice-chatbot
```

2. **Установите зависимости:**

Установите необходимые Python-библиотеки:

```bash
pip install -r requirements.txt
```

Либо, если вы используете Google Colab, установите библиотеки так:

```python
!pip install gradio groq-api openai-whisper gtts
```

## Конфигурация

### Настройка Groq API Key

Добавьте ваш ключ Groq API в переменные окружения:

```bash
export GROQ_API_KEY='your_groq_api_key'
```

В Google Colab ключ можно задать так:

```python
import os
os.environ['GROQ_API_KEY'] = 'your_groq_api_key'
```

### Важное замечание по запуску (текущее поведение кода)

Текущий скрипт инициализирует клиент Groq с жёстко заданной заглушкой:

```python
client = Groq(api_key="your_groq_api_key")
```

Если вы только зададите `GROQ_API_KEY` в окружении, обновите скрипт так, чтобы он читал ключ из `os.environ`, или замените заглушку напрямую, иначе аутентификация API завершится ошибкой.

## Использование

Чтобы запустить чат-бота, выполните основной скрипт:

```bash
python voice_to_voice_chatbot.py
```

Или в Google Colab:

Скопируйте скрипт в ячейку кода и выполните.

Интерфейс Gradio будет запущен, и вы сможете взаимодействовать с чат-ботом.

### Взаимодействие с чат-ботом

- **Использование микрофона**: Говорите прямо в микрофон. Чат-бот распознает вашу речь, сгенерирует ответ и воспроизведёт его как аудио.
- **Загрузка аудио**: Загрузите заранее записанный аудиофайл. Чат-бот распознает аудио, сгенерирует ответ и преобразует его обратно в речь.

## Примеры

### Пример голосового сценария

1. Запись: "What are three tips to learn Python quickly?"
2. Whisper транскрибирует ваш текстовый запрос.
3. Модель Groq LLaMA генерирует ответ.
4. gTTS формирует MP3-ответ.
5. Gradio отображает текст ответа и воспроизводит аудио.

### Пример команды запуска

```bash
python voice_to_voice_chatbot.py
```

Ожидаемый результат: в браузере откроется локальное приложение Gradio с одним аудиовходом и двумя выходами (текст + аудио).

## Заметки по разработке

- Основная функция конвейера: `chatbot_pipeline(audio_path)`.
- Модель Whisper загружается при запуске: `whisper.load_model("base")`.
- Временные MP3-файлы создаются через `NamedTemporaryFile(..., delete=False)`.
- Текущая обработка ошибок возвращает в UI `(str(e), None)`.
- Зависимости в `requirements.txt` включают и `whisper`, и `openai-whisper`; в зависимости от среды это может быть избыточно.

## Устранение неполадок

### Частые проблемы

`ModuleNotFoundError`: Убедитесь, что установлена корректная версия модуля Whisper:

```python
!pip install -U openai-whisper
```

`Groq API Key Error`: Ещё раз проверьте API-ключ и убедитесь, что он правильно задан в переменных окружения.

Дополнительная диагностика:

- Если приложение сразу падает с ошибками аутентификации, проверьте жёстко заданный `api_key="your_groq_api_key"` в `voice_to_voice_chatbot.py`.
- Если захват с микрофона недоступен, сначала загрузите аудиофайл, чтобы проверить конвейер STT -> LLM -> TTS.
- Если аудио-ответ пустой, проверьте наличие исходящего сетевого доступа для gTTS.

### Краткий чек-лист диагностики

| Проверка | Валидация |
|---|---|
| Подключение API-ключа | `Groq(api_key=...)` не оставлен как заглушка |
| Установка Whisper | `openai-whisper` импортируется без ошибок |
| Сетевой доступ | Есть исходящий доступ к Groq + gTTS |
| Источник аудио | Разрешения микрофона включены или работает загрузка файла |

## Дорожная карта

- По умолчанию читать Groq API key напрямую из переменных окружения.
- Добавить тесты для вспомогательных функций конвейера.
- Добавить опциональные настройки модели/конфига через переменные окружения или аргументы CLI.
- Добавить i18n README-файлы в `i18n/` в соответствии со ссылками языковой навигации.
- Добавить варианты деплоя (например, Docker или Hugging Face Spaces).

## Вклад

Вклад приветствуется. Сделайте форк этого репозитория, создайте новую ветку и отправьте pull request с вашими изменениями.

Рекомендуемый процесс внесения изменений:

1. Сделайте форк и склонируйте репозиторий.
2. Создайте feature-ветку.
3. Внесите изменения и протестируйте их.
4. Откройте pull request с понятным описанием.

## Поддержка

В текущем содержимом репозитория не найдено явных ссылок на донаты/спонсорство. Если мейнтейнеры добавят каналы поддержки, их стоит указать здесь.

## Лицензия

Этот проект распространяется по лицензии MIT. Подробности см. в файле LICENSE.

Предположение: файл `LICENSE` подразумевается, но в этом снимке репозитория может пока отсутствовать.
