[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# روبوت محادثة صوت-إلى-صوت باستخدام Whisper وLLaMA وGroq API

![Python](https://img.shields.io/badge/Python-3.7%2B-3776AB?logo=python&logoColor=white)
![Whisper](https://img.shields.io/badge/STT-OpenAI%20Whisper-1f6feb)
![Groq](https://img.shields.io/badge/LLM-Groq%20LLaMA%203--8B-FF6B35)
![gTTS](https://img.shields.io/badge/TTS-Google%20gTTS-34A853)
![UI](https://img.shields.io/badge/UI-Gradio-F97316)
![Status](https://img.shields.io/badge/Project-Active%20Prototype-2ea44f)

هذا المشروع عبارة عن روبوت محادثة صوت-إلى-صوت يعمل في الزمن الحقيقي، ويعتمد على Whisper من OpenAI لتحويل الكلام إلى نص، وعلى LLaMA 8B عبر Groq API لتوليد الردود، وعلى Google Text-to-Speech ‏(gTTS) لتحويل النص مرة أخرى إلى صوت. واجهة الروبوت مبنية باستخدام Gradio، ما يتيح للمستخدمين التفاعل معه عبر التحدث المباشر أو رفع ملفات صوتية.

## نظرة عامة

يطبّق التطبيق حلقة محادثة صوتية كاملة داخل سكربت Python واحد:

1. استقبال صوت المستخدم من الميكروفون أو من ملف مرفوع.
2. تحويل الكلام إلى نص باستخدام Whisper (نموذج `base`).
3. توليد رد عبر Groq (`llama3-8b-8192`).
4. تحويل نص الرد إلى صوت MP3 باستخدام gTTS.
5. إرجاع نص الرد والصوت القابل للتشغيل داخل واجهة Gradio على الويب.

### خط أنابيب المحادثة

| المرحلة | المكوّن | المخرجات |
|---|---|---|
| 🎙️ الإدخال | Gradio Audio (mic/file) | مسار الصوت |
| 📝 التفريغ النصي | Whisper `base` | نص المستخدم |
| 🧠 الاستدلال | Groq + `llama3-8b-8192` | نص المساعد |
| 🔊 تركيب الصوت | gTTS | رد MP3 |
| 🖥️ التسليم | Gradio UI | نص + صوت قابل للتشغيل |

## الميزات

- **تحويل الكلام إلى نص**: تحويل اللغة المنطوقة إلى نص باستخدام نموذج Whisper من OpenAI.
- **ردود مولّدة بالذكاء الاصطناعي**: استخدام LLaMA 8B عبر Groq API لتوليد ردود ذكية اعتمادًا على النص المُفرّغ.
- **تحويل النص إلى كلام**: تحويل الردود النصية المولّدة إلى صوت باستخدام Google Text-to-Speech ‏(gTTS).
- **تفاعل في الزمن الحقيقي**: يعمل الروبوت لحظيًا، ما يسمح بالتفاعل عبر الميكروفون أو برفع ملفات صوتية من خلال واجهة ويب.
- **تشغيل بسيط بملف واحد**: خط الأنابيب الكامل للروبوت مُنفّذ في `voice_to_voice_chatbot.py` لتسهيل التجربة.

## بنية المشروع

```text
Voice-to-text-and-voice-chatbot/
├── voice_to_voice_chatbot.py               # السكربت الرئيسي لتشغيل روبوت المحادثة
├── requirements.txt                        # قائمة تبعيات Python
├── README.md                               # توثيق المشروع (الإنجليزية)
├── i18n/                                   # ملفات README المترجمة (مخطط/مولد ضمن خط الأنابيب)
└── .auto-readme-work/20260228_230442/      # سياق/مخرجات أتمتة README
```

مرجع البنية القديمة من README المرجعي:

```text
voice-to-voice-chatbot/
├── voice_to_voice_chatbot.py  # السكربت الرئيسي لتشغيل روبوت المحادثة
├── requirements.txt           # قائمة تبعيات Python
├── README.md                  # توثيق المشروع
└── .gitignore                 # ملف تجاهل Git
```

## المتطلبات المسبقة

قبل البدء، تأكد من استيفاء المتطلبات التالية:

- تثبيت Python 3.7 أو أحدث على جهازك المحلي أو على Google Colab.
- امتلاك مفتاح Groq API. يمكنك التسجيل للحصول على مفتاح API من [هنا](https://groq.com/).
- [Google Colab](https://colab.research.google.com/) أو بيئة Python محلية تحتوي على المكتبات المطلوبة.
- اتصال بالإنترنت من أجل:
  - تنزيل نموذج Whisper لأول مرة.
  - استدعاءات Groq API.
  - توليد الصوت عبر gTTS.

### المتطلبات بنظرة سريعة

| المتطلب | سبب الحاجة إليه |
|---|---|
| Python `3.7+` | بيئة تشغيل سكربت الروبوت والتبعيات |
| Groq API Key | وصول موثّق لاستدلال نموذج LLaMA |
| Colab or Local Env | بيئة تنفيذ لـ Gradio + مكتبات ML |
| Internet Access | تنزيل نموذج Whisper، طلبات Groq، توليد gTTS |

## التثبيت

اتبع هذه الخطوات لإعداد المشروع:

1. **استنساخ المستودع:**

```bash
git clone https://github.com/aquibali01/voice-to-voice-chatbot.git
cd voice-to-voice-chatbot
```

2. **تثبيت التبعيات:**

ثبّت مكتبات Python المطلوبة:

```bash
pip install -r requirements.txt
```

بديلًا عن ذلك، إذا كنت تستخدم Google Colab، يمكنك تثبيت المكتبات باستخدام:

```python
!pip install gradio groq-api openai-whisper gtts
```

## الإعداد

### إعداد Groq API Key

أضف مفتاح Groq API إلى متغيرات البيئة:

```bash
export GROQ_API_KEY='your_groq_api_key'
```

في Google Colab، يمكنك تعيين مفتاح API باستخدام:

```python
import os
os.environ['GROQ_API_KEY'] = 'your_groq_api_key'
```

### ملاحظة تشغيل مهمة (سلوك الكود الحالي)

السكربت الحالي يهيّئ عميل Groq باستخدام عنصر نائب مُشفّر مباشرة:

```python
client = Groq(api_key="your_groq_api_key")
```

إذا كنت تضبط `GROQ_API_KEY` في البيئة فقط، فحدّث السكربت ليقرأ من `os.environ` أو استبدل العنصر النائب مباشرة، وإلا ستفشل مصادقة API.

## الاستخدام

لتشغيل روبوت المحادثة، شغّل السكربت الرئيسي:

```bash
python voice_to_voice_chatbot.py
```

أو في Google Colab:

انسخ السكربت إلى خلية كود ثم نفّذه.

ستُطلق واجهة Gradio، ما يتيح لك التفاعل مع روبوت المحادثة.

### التفاعل مع روبوت المحادثة

- **استخدام الميكروفون**: تحدث مباشرة في الميكروفون. سيقوم الروبوت بتفريغ كلامك، وتوليد رد، وتشغيله كصوت.
- **رفع ملف صوتي**: ارفع ملفًا صوتيًا مسجلًا مسبقًا. سيقوم الروبوت بتفريغ الصوت، وتوليد رد، ثم تحويل الرد إلى كلام.

## أمثلة

### مثال على تدفق صوتي

1. سجّل: "What are three tips to learn Python quickly?"
2. يقوم Whisper بتفريغ نص سؤالك.
3. يولد نموذج Groq LLaMA إجابة.
4. ينتج gTTS ردًا بصيغة MP3.
5. يعرض Gradio نص الرد وإمكانية تشغيل الصوت.

### مثال أمر التشغيل

```bash
python voice_to_voice_chatbot.py
```

النتيجة المتوقعة: يفتح تطبيق Gradio محلي في المتصفح مع مدخل صوتي واحد ومخرجين (نص + صوت).

## ملاحظات التطوير

- دالة خط الأنابيب الرئيسية: `chatbot_pipeline(audio_path)`.
- يتم تحميل نموذج Whisper عند بدء التشغيل: `whisper.load_model("base")`.
- تُنشأ ملفات MP3 مؤقتة عبر `NamedTemporaryFile(..., delete=False)`.
- معالجة الأخطاء تُرجع حاليًا `(str(e), None)` إلى الواجهة.
- التبعيات في `requirements.txt` تتضمن كلًا من `whisper` و`openai-whisper`؛ قد يكون ذلك زائدًا حسب البيئة.

## استكشاف الأخطاء وإصلاحها

### مشكلات شائعة

`ModuleNotFoundError`: تأكد من أنك ثبّت النسخة الصحيحة من وحدة Whisper باستخدام

```python
!pip install -U openai-whisper
```

`Groq API Key Error`: راجع مفتاح API وتأكد من ضبطه بشكل صحيح في متغيرات البيئة.

استكشاف إضافي للأخطاء:

- إذا فشل التطبيق مباشرة مع أخطاء مصادقة، فتحقق من `api_key="your_groq_api_key"` المضمّن مباشرة في `voice_to_voice_chatbot.py`.
- إذا لم يتوفر التقاط الميكروفون، ارفع ملفًا صوتيًا أولًا للتحقق من خط الأنابيب STT -> LLM -> TTS.
- إذا كان صوت الرد فارغًا، تأكد من توفر وصول شبكي صادر لـ gTTS.

### قائمة تشخيص سريعة

| الفحص | التحقق |
|---|---|
| توصيل مفتاح API | `Groq(api_key=...)` ليس عنصرًا نائبًا |
| تثبيت Whisper | `openai-whisper` يُستورد دون مشاكل |
| المسار الشبكي | يتوفر وصول صادر لـ Groq + gTTS |
| مصدر الصوت | أذونات الميكروفون مفعلة أو رفع الملف يعمل |

## خارطة الطريق

- قراءة مفتاح Groq API مباشرة من متغيرات البيئة افتراضيًا.
- إضافة اختبارات لدوال خط الأنابيب المساعدة.
- إضافة إعدادات نموذج/ضبط اختيارية عبر متغيرات البيئة أو وسيطات CLI.
- إضافة ملفات README مترجمة تحت `i18n/` بما يطابق روابط التنقل بين اللغات.
- إضافة خيارات نشر (مثلًا Docker أو Hugging Face Spaces).

## المساهمة

المساهمات مرحب بها. يُرجى عمل fork لهذا المستودع، وإنشاء فرع جديد، ثم إرسال pull request يتضمن تغييراتك.

تدفق مساهمة مقترح:

1. اعمل fork ثم clone للمستودع.
2. أنشئ فرع ميزة.
3. نفّذ تغييراتك واختبرها.
4. افتح pull request مع وصف واضح.

## الدعم

لم يتم العثور على روابط تبرع/رعاية صريحة في محتوى المستودع الحالي. إذا أضاف المشرفون قنوات دعم، فينبغي إدراجها هنا.

## الترخيص

هذا المشروع مرخّص بموجب رخصة MIT. راجع ملف LICENSE للحصول على التفاصيل.

افتراض: يُفترض وجود ملف `LICENSE` لكنه قد لا يكون موجودًا بعد في هذه اللقطة من المستودع.
